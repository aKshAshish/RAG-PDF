import logo from "../assets/img/logo.png";

const Header = () => {
    return (
        <div id="header">
            <img id="logo" src={logo}></img>
        </div>
    );
};

export default Header;
